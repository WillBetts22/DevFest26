import type { Message } from "./types/messages.mjs";
import type { ToolResult, Tool } from "./types/tools.mjs";
import type { Dedalus } from "../../client.mjs";
import type { CompletionCreateParamsBase } from "../../resources/chat/completions.mjs";
import type { DedalusModelChoice } from "../../resources/shared.mjs";
export type RunParams = Omit<CompletionCreateParamsBase, 'messages' | 'model' | 'tools' | 'mcp_servers' | 'auto_execute_tools'> & {
    input?: string | Message[];
    messages?: Message[];
    instructions?: string;
    model: DedalusModelChoice | DedalusModelChoice[] | undefined;
    tools?: Iterable<Tool>;
    maxSteps?: number;
    mcpServers?: Iterable<string>;
    autoExecuteTools?: boolean;
    responseFormat?: {
        [key: string]: unknown;
    };
    transport?: 'http';
    verbose?: boolean;
    debug?: boolean;
    returnIntent?: boolean;
};
export declare class RunResult {
    finalOutput: string;
    toolResults: ToolResult[];
    stepsUsed: number;
    conversationHistory: Message[];
    toolsCalled: string[];
    modelsUsed: DedalusModelChoice[];
    constructor(finalOutput: string, toolResults: ToolResult[], stepsUsed: number, conversationHistory?: Message[], toolsCalled?: string[], modelsUsed?: DedalusModelChoice[]);
    get output(): string;
    get content(): string;
    toInputList(): Message[];
}
/** Client wrapper with local tool execution and multi-turn conversation support. */
export declare class DedalusRunner {
    private client;
    private verbose;
    constructor(client: Dedalus, verbose?: boolean);
    /** Executes conversation with automatic tool calling up to maxSteps. */
    run(params: RunParams): Promise<RunResult | AsyncIterableIterator<any>>;
    /** Executes synchronous multi-turn conversation with tool support. */
    private runTurns;
    /** Executes streaming conversation, yielding content deltas. */
    private runStreaming;
    /** Executes local tools and appends results to conversation. */
    private executeToolCallsSync;
}
//# sourceMappingURL=runner.d.mts.map